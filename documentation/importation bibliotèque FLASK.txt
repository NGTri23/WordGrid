- vérifie que tu as bien mis le fichier app.py dans le même endroit qu'il y a le dossier dictionnaire
- assure toi que le dossier templates est aussi dans le même dossier que le fichier app.py (IMPORTANT)
- ouvre le fichier app.py avec thonny (ou autre logiciel avec du python et un console)
- dans la console de thonny, entre (sans les guillemets) : '!pip install Flask'
(Vérifie qu'à la fin de l'execution que thonny a bien installé flask, sinon contacte et demande à Tri)

- ensuite, tu peux exécuter le fichier app.py
Si le code lance :
Dans la console, il devrait t'indiquer une URL. Entre la dans un naviguateur

En cas de n'importe quelle erreur, dit à Tri