/ I) LA COMPOSITION DES FICHIERS /

wordgrid/
│
├── main.py                 # point d’entrée (lance le jeu)
├── config.py               # configuration de la partie
├── game.py                 # logique du jeu (WordGrid)
├── dictionary.py           # gestion des dictionnaires
├── ui.py                   # interface pygame
│
├── data/
│   ├── fr_nature.txt
│   ├── fr_all.txt
│   ├── en_nature.txt
│   └── en_all.txt
│
└── assets/                # images, polices, sons si besoin


/ II) LA PARTIE 1 - LA LOGIQUE DU JEU /

class GameConfig:
    def __init__(self, lang, theme, length, max_attempts, chrono=False):
        self.lang = lang
        self.theme = theme
        self.length = length
        self.max_attempts = max_attempts
        self.chrono = chrono

class DictionaryManager:
    def __init__(self, data_path="data"):
        self.data_path = data_path

    def load_words(self, lang, theme):
        filename = f"{lang}_{theme}.txt"
        with open(f"{self.data_path}/{filename}", "r", encoding="utf-8") as f:
            return [line.strip().upper() for line in f]

    def filter_by_length(self, words, length):
        return [w for w in words if len(w) == length]

class WordGridGame:
    def __init__(self, config, words):
        self.config = config
        self.secret = random.choice(words)
        self.attempts = []
        self.used_letters = set()
        self.finished = False
        self.won = False

	# Méthodes importantes :
	def check_guess(self, guess): ...
	def is_valid_guess(self, guess, words): ...
	def is_over(self): ...

/ III) L'INTERFACE AVEC PYGAME /
- L'ensemble de la création des inter
(en écriture)